# Super Admin Architecture Diagram

## System Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                        SUPER ADMIN INTERFACE                         │
│                        /admin/* pages                                │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                          ADMIN LAYOUT                                │
│  ┌────────────────────────────────────────────────────────────┐    │
│  │ ⚠️ Super Admin Warning Banner                              │    │
│  │ "You have full access to all organizations"                │    │
│  └────────────────────────────────────────────────────────────┘    │
│  ┌────────────────────────────────────────────────────────────┐    │
│  │ 🏢 Organization Selector                                    │    │
│  │ [ All Organizations (Platform View) ▼ ]                    │    │
│  └────────────────────────────────────────────────────────────┘    │
│                                                                      │
│  ┌──────────────┐  ┌────────────────────────────────────────┐     │
│  │   Sidebar    │  │   Main Content Area                      │     │
│  │              │  │   (OrganizationContext.Provider)        │     │
│  │  Dashboard   │  │                                          │     │
│  │  Stories     │  │   {children} ← Admin pages render here  │     │
│  │  Users       │  │                                          │     │
│  │  Projects    │  │                                          │     │
│  │  ...         │  │                                          │     │
│  └──────────────┘  └────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────────────────────┘
```

## Data Flow: Organization Selection

```
User selects "Oonchiumpa" from dropdown
            │
            ▼
┌───────────────────────────────────┐
│  OrganizationSelector Component   │
│  onChange(orgId) triggered         │
└───────────────────────────────────┘
            │
            ▼
┌───────────────────────────────────┐
│  OrganizationContext              │
│  setSelectedOrgId(orgId)          │
│  State updates to new orgId       │
└───────────────────────────────────┘
            │
            ▼
┌───────────────────────────────────┐
│  AdminDashboard Component         │
│  useOrganizationContext()         │
│  useEffect(() => {}, [selectedOrgId]) │
│  Detects change, triggers refetch │
└───────────────────────────────────┘
            │
            ▼
┌───────────────────────────────────┐
│  API Call                         │
│  if (selectedOrgId === 'all')     │
│    GET /api/admin/stats/platform  │
│  else                             │
│    GET /api/admin/organizations/  │
│        {orgId}/stats              │
└───────────────────────────────────┘
            │
            ▼
┌───────────────────────────────────┐
│  API Route Handler                │
│  requireSuperAdminAuth()          │
│  createServiceRoleClient()        │
└───────────────────────────────────┘
            │
            ▼
┌───────────────────────────────────┐
│  Database Query                   │
│  WITH organization_id filter      │
│  (via helper functions)           │
└───────────────────────────────────┘
            │
            ▼
┌───────────────────────────────────┐
│  Response Data                    │
│  Organization-specific stats      │
└───────────────────────────────────┘
            │
            ▼
┌───────────────────────────────────┐
│  UI Update                        │
│  Dashboard shows Oonchiumpa data  │
│  Header: "Oonchiumpa Dashboard"   │
└───────────────────────────────────┘
```

## API Architecture

### Platform Stats Flow (All Organizations)

```
┌──────────────────────────────────────────────────────────────┐
│  User selects: "All Organizations (Platform View)"           │
└──────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌──────────────────────────────────────────────────────────────┐
│  GET /api/admin/stats/platform                               │
│                                                               │
│  1. requireSuperAdminAuth(request)                           │
│     - Verify user is benjamin@act.place                      │
│     - Check is_super_admin flag                              │
│                                                               │
│  2. createServiceRoleClient()                                │
│     - Bypasses Row Level Security                            │
│     - Full database access                                   │
│                                                               │
│  3. Query ALL organizations                                  │
│     SELECT * FROM organizations                              │
│     ORDER BY name                                            │
│                                                               │
│  4. Aggregate stats across ALL orgs                          │
│     SELECT * FROM stories                                    │
│     SELECT * FROM transcripts                                │
│     SELECT * FROM profile_organizations                      │
│     (NO organization_id filter)                              │
│                                                               │
│  5. Calculate totals                                         │
│     - Total organizations: 18                                │
│     - Total stories: 301                                     │
│     - Stories by status (draft, review, published, etc)      │
│     - Total transcripts: 222                                 │
│     - Total members: 247                                     │
└──────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌──────────────────────────────────────────────────────────────┐
│  Response: Platform-wide aggregated stats                    │
│  {                                                            │
│    platform: {                                               │
│      totalOrganizations: 18,                                 │
│      stories: { total: 301, draft: 3, ... },                 │
│      transcripts: { total: 222 },                            │
│      members: { total: 247 }                                 │
│    },                                                         │
│    organizations: [ ... ]                                    │
│  }                                                            │
└──────────────────────────────────────────────────────────────┘
```

### Organization-Specific Stats Flow

```
┌──────────────────────────────────────────────────────────────┐
│  User selects: "Oonchiumpa"                                  │
│  orgId: c53077e1-98de-4216-9149-6268891ff62e                │
└──────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌──────────────────────────────────────────────────────────────┐
│  GET /api/admin/organizations/[orgId]/stats                  │
│                                                               │
│  1. requireSuperAdminAuth(request)                           │
│     - Verify super admin access                              │
│                                                               │
│  2. createServiceRoleClient()                                │
│     - Service role = bypass RLS                              │
│     - Can access ANY organization                            │
│                                                               │
│  3. Verify organization exists                               │
│     SELECT * FROM organizations                              │
│     WHERE id = 'c53077e1-...'                               │
│                                                               │
│  4. getOrganizationDashboardStats(supabase, orgId)          │
│     ┌────────────────────────────────────────────┐          │
│     │  SELECT * FROM stories                     │          │
│     │  WHERE organization_id = 'c53077e1-...'    │ ← FILTER │
│     │                                             │          │
│     │  SELECT * FROM transcripts                 │          │
│     │  WHERE organization_id = 'c53077e1-...'    │ ← FILTER │
│     │                                             │          │
│     │  SELECT * FROM blog_posts                  │          │
│     │  WHERE organization_id = 'c53077e1-...'    │ ← FILTER │
│     │                                             │          │
│     │  SELECT * FROM organization_members        │          │
│     │  WHERE organization_id = 'c53077e1-...'    │ ← FILTER │
│     └────────────────────────────────────────────┘          │
│                                                               │
│  5. Aggregate Oonchiumpa stats only                         │
│     - Stories: 6 (all published)                             │
│     - Transcripts: 0                                         │
│     - Projects: 2                                            │
└──────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌──────────────────────────────────────────────────────────────┐
│  Response: Oonchiumpa-specific stats                         │
│  {                                                            │
│    organizationId: "c53077e1-...",                          │
│    organizationName: "Oonchiumpa",                          │
│    organizationSlug: "oonchiumpa",                          │
│    stories: { total: 6, published: 6, ... },                │
│    transcripts: { total: 0 },                               │
│    projects: { total: 2 }                                   │
│  }                                                            │
└──────────────────────────────────────────────────────────────┘
```

## Database Schema: Multi-Tenant Structure

```
┌─────────────────────────────────────────────────────────────┐
│                      organizations                           │
├─────────────────────────────────────────────────────────────┤
│  id (PK)              UUID                                   │
│  name                 TEXT      "Oonchiumpa"                 │
│  slug                 TEXT      "oonchiumpa"                 │
│  created_at           TIMESTAMP                              │
└─────────────────────────────────────────────────────────────┘
                           │
                           │ 1:N
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                         stories                              │
├─────────────────────────────────────────────────────────────┤
│  id (PK)              UUID                                   │
│  organization_id (FK) UUID      → organizations.id          │
│  title                TEXT                                   │
│  status               TEXT      draft/review/published       │
│  storyteller_id (FK)  UUID                                   │
│  ...                                                          │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                      transcripts                             │
├─────────────────────────────────────────────────────────────┤
│  id (PK)              UUID                                   │
│  organization_id (FK) UUID      → organizations.id          │
│  storyteller_id (FK)  UUID                                   │
│  text                 TEXT                                   │
│  ...                                                          │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                   organization_members                       │
├─────────────────────────────────────────────────────────────┤
│  id (PK)              UUID                                   │
│  organization_id (FK) UUID      → organizations.id          │
│  profile_id (FK)      UUID      → profiles.id               │
│  is_active            BOOLEAN                                │
│  ...                                                          │
└─────────────────────────────────────────────────────────────┘
```

## Authentication & Authorization Flow

```
┌──────────────────────────────────────────────────────────────┐
│                  HTTP Request to Admin API                    │
│                  GET /api/admin/...                           │
└──────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌──────────────────────────────────────────────────────────────┐
│              requireSuperAdminAuth(request)                   │
│                                                               │
│  1. Check authentication                                      │
│     ┌─────────────────────────────────────────────┐         │
│     │  supabase.auth.getUser()                    │         │
│     │  - Validates session cookie                 │         │
│     │  - Returns user object or error             │         │
│     └─────────────────────────────────────────────┘         │
│                                                               │
│  2. Get user profile                                         │
│     ┌─────────────────────────────────────────────┐         │
│     │  SELECT * FROM profiles                     │         │
│     │  WHERE id = user.id                         │         │
│     └─────────────────────────────────────────────┘         │
│                                                               │
│  3. Check super admin status                                 │
│     ┌─────────────────────────────────────────────┐         │
│     │  if (email === 'benjamin@act.place')        │         │
│     │    is_super_admin = true                    │         │
│     └─────────────────────────────────────────────┘         │
│                                                               │
│  4. Verify permissions                                       │
│     ┌─────────────────────────────────────────────┐         │
│     │  if (!is_super_admin)                       │         │
│     │    return 403 Forbidden                     │         │
│     └─────────────────────────────────────────────┘         │
└──────────────────────────────────────────────────────────────┘
                           │
                           ▼ (if authorized)
┌──────────────────────────────────────────────────────────────┐
│                   API Route Handler                           │
│                   (can now use service role client)           │
└──────────────────────────────────────────────────────────────┘
```

## Component Hierarchy

```
AdminLayout (src/app/admin/layout.tsx)
│
├── Super Admin Warning Banner
│   └── Alert: "You have full access to all organizations"
│
├── Header
│   ├── Title: "Admin Panel"
│   ├── Subtitle: Dynamic based on selectedOrgId
│   └── OrganizationSelector
│       └── Dropdown: All orgs + "All Organizations" option
│
├── OrganizationContext.Provider
│   │  value={{ selectedOrgId, setSelectedOrgId }}
│   │
│   └── Main Content Area
│       │
│       └── {children} ← Admin pages
│           │
│           ├── AdminDashboard (src/components/admin/AdminDashboard-simple.tsx)
│           │   │  useOrganizationContext()
│           │   │
│           │   ├── useEffect(() => { fetchStats() }, [selectedOrgId])
│           │   │
│           │   ├── Stats Cards
│           │   │   ├── Total Users
│           │   │   ├── Total Stories
│           │   │   ├── Pending Reviews
│           │   │   └── Organizations
│           │   │
│           │   └── Quick Actions
│           │       ├── Manage Users
│           │       ├── Content
│           │       ├── Organizations
│           │       └── Reviews
│           │
│           ├── Stories Page (future)
│           ├── Users Page (future)
│           └── Projects Page (future)
│
└── Sidebar
    ├── Dashboard
    ├── Users
    ├── Stories
    ├── Organizations
    └── Projects
```

## Data Isolation Guarantee

```
Organization A           Organization B           Organization C
(Oonchiumpa)            (A Curious Tractor)      (Community Elder)
     │                        │                        │
     ├── 6 stories            ├── 0 stories            ├── 10 stories
     ├── 0 transcripts        ├── 0 transcripts        ├── 0 transcripts
     └── 2 projects           └── 1 project            └── 0 projects
     │                        │                        │
     │                        │                        │
     └────────────────────────┼────────────────────────┘
                              │
                              │ ALL filtered by organization_id
                              ▼
                    ┌─────────────────────┐
                    │   Database Queries   │
                    │                      │
                    │  WHERE                │
                    │  organization_id = X │
                    └─────────────────────┘
                              │
                              ▼
                    🔒 100% Data Isolation
                    ✅ No Cross-Contamination
                    ✅ Verified through testing
```

## Summary

This architecture provides:

1. **Complete Separation** - Each organization's data is isolated
2. **Super Admin Access** - Can view/edit ANY organization
3. **Flexible Viewing** - Platform-wide OR organization-specific
4. **Clear Context** - Always know which org you're viewing
5. **Secure Access** - Service role client for super admin only
6. **Best Practices** - Helper functions enforce filtering
